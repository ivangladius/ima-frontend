import 'package:flutter/material.dart';

class PowertrainTab extends StatelessWidget {
  const PowertrainTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Powertrain Tab'),
    );
  }
}
